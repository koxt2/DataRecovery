# Polkit action for running ddrescue with elevated privileges
# This action is intentionally set to auth_admin for security
# setBadness reduces the severity from error to warning
setBadness('polkit-untracked-privilege', 0)
